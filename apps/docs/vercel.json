{
  "buildCommand": "cd ../.. && npm install && npm run build:pkg && npm run build -w docs",
  "outputDirectory": "build/client",
  "installCommand": "npm install --prefix ../..",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server"
    }
  ]
}
